============
Installation
============

At the command line::

    $ pip install ceph_bridge

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ceph_bridge
    $ pip install ceph_bridge
